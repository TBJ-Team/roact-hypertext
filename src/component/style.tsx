import * as Roact from "@rbxts/roact";

export type Style = {

}

export const StyleContext = Roact.createContext({});

export default function link() {
	return (<StyleContext.Provider>

	</StyleContext.Provider>);
}